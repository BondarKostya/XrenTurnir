<Window x:Class="SeinSport.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeinSport"
        xmlns:localGrids="clr-namespace:SeinSport.Grids"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="588.157" ResizeMode="CanResize"
        Loaded="Window_Loaded">
    <TabControl TabStripPlacement="Bottom" x:Name="tabControl" Margin="20" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <TabItem Header="Муж">            
                <!--<localGrids:CategoryGrid x:Name="mainGrid" Background="#FFE5E5E5" ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.CanContentScroll="True">
                </localGrids:CategoryGrid>-->
            <StackPanel>
                <Button Content="ToLast" Click="Button_Click" Margin="5"/>
                <StackPanel x:Name="sportPanel">
                    <!--<local:SportListView x:Name="sportView"/>
                <local:SportListView x:Name="testView"/>-->
                </StackPanel>
            </StackPanel>
        </TabItem>
        <TabItem Header="Жен">
            <Grid Background="#FFE5E5E5"/>
        </TabItem>
    </TabControl>
</Window>
